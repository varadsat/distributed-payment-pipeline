// Package observability holds logging and metrics helpers.
package observability

// TODO:
//   - Logger: structured JSON logging (slog) with a correlation_id field.
//   - Metrics: submission rate, validation failure rate, dedup hit rate,
//     publish latency, outbox relay lag, per-state counts.
